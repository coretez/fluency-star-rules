id: RC-08.01/v1
contract: RC-08.01
variation: v1
name: High-risk-jurisdiction AI domain resolved
platform: sentinelone
os:
- windows
- macos
- linux
primitive: TP-DNS
severity: high
status: active
refs:
  domains:
  - tier_a_jurisdiction
query: 'event.type = "DNS Resolved"

  AND event.dns.request in contains anycase ({{ domains:tier_a_jurisdiction }})

  '
