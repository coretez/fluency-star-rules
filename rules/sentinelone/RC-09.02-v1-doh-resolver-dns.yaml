id: RC-09.02/v1
contract: RC-09.02
variation: v1
name: DoH resolver hostname resolved
platform: sentinelone
os:
- windows
- macos
- linux
primitive: TP-DNS
severity: medium
status: active
refs:
  domains:
  - doh_resolvers
query: 'event.type = "DNS Resolved"

  AND event.dns.request in contains anycase ({{ domains:doh_resolvers }})

  '
